@*@using System.Web.Optimization;
    @using SEMYPROJECT.Public;

    @Scripts.Render(VirtualPathStrings.jQuery)  *@
@* Razor for .cshtml : procceeded by an @@ *@

@{ int i, j, k, sum;
    i = 99;
    j = -1;
    k = 1;
    sum = 0;
    string name = " Zahidul alam";
    string sex = "male";
    int age = 22;
    sum = i + j;
}

<b> Sum = @sum</b>
@if (sex == "male")
{
    <b>Welcome genteleman @name</b>


}
else
{
    <b>Welcome Ms , @name</b>
}


<hr />


@if (age < 20)
{
    <b> You are ypung </b>
}
else
{
    <b> youre old</b>
}


<hr />
Welcome a person by Viewbag!! <br /> </br>
@{ 
    User1 u = ViewBag.Xuser as User1;
}
Welcome ! Your information is :<br />
name:@u.user<br />
password:@u.pwd<br />
age: @u.age<br />


           <div class="container">
               <div class="row">
                   <div class="col-md-2">
                       Time:
                   </div>
                   <div class="col-md-10">
                       @DateTime.Now.ToString("t")
                       <br />
                       @DateTime.Now.ToString("T")
                   </div>

                   <div class="col-md-2">
                       Date:
                   </div>
                   <div class="col-md-10">
                       @DateTime.Now.ToString("d")
                       <br />
                       @DateTime.Now.ToString("D")
                   </div>

               </div>

               <div class="row">
                   <div class="col-md-2">
                     Welcome:
                   </div>
                   <div class="col-md-10">
                       @if (DateTime.Now.Hour < 12)
                       {
                           <span>Good morning</span>
                       }
                       else if (DateTime.Now.Hour < 13 && DateTime.Now.Hour > 12)
                       {
                           <span>Good noon</span>
                       }
                   </div>
               </div>
              
